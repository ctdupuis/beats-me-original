<%= form_for @song, url: album_song_path(@song.album, @song)  do |f| %>


    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.label :length %>
    <%= f.text_field :length %>

    <%= f.hidden_field :album_id %>
   

    <%= f.submit %>
<% end %>