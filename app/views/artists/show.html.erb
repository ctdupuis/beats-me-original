<div class="container-lined">
    <h1><%= @artist.name %></h1>
</div>



<h2>Discography</h2>
<% @artist.albums.each do |album| %>
    <div class="list-item">
        <p><%= link_to album.name, album_path(album) %><p>
    </div>
<% end %>